#include "systemc.h"
#include "server.h"

void server::assignment(){
	// struct motionData robot = location;
	// bool obs = obstacle;
	// int d = robot.direction;
	// bool reached=false;

	// if (robot.location[0] >= 6.5 && robot.location[1] == -0.5)
	// 	d = 1;
	// direction = d;

	// if (envir.getGridNumber(&robot) == 10)
	// {
	// 	reached = true;
	// }

	// if (obs)
	// {
	// 	cout <<"stopped because of human incoming." << endl;
	// }

	// if (obs || reached)
	// {
	// 	stop = true;
	// }
	// else
	// {
	// 	stop = false;
	// }
	
} 
